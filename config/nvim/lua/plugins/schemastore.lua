return {
  "b0o/SchemaStore.nvim",
  -- Carregado sob demanda pelo require("schemastore") dentro dos after/lsp/.
  lazy = true,
  -- A última release tagueada é muito antiga; o catálogo útil está no default branch.
  version = false,
}
